name: pinput
description: Pin code input (OTP) text field, Supports custom numpad. Password,Passcode,Captcha,Security,Coupon,Wowcher,2FA,Two step verification,two-factor authentication
version: 2.0.3
homepage: https://tkko.github.io/Flutter_PinPut
repository: https://github.com/Tkko/Flutter_PinPut


environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"


dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.8.2